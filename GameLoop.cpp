#include "GameLoop.h"

GameLoop::Gameloop(int numRounds){
    roundsPerGame = numRounds}

GameLoop::Play()
{
    int score1, score2, lastRoll1, lastRoll2 = 0;

    for (int rnd i = 0; i < roundsPerGame; rnd++)
    {
        // the first player's turn
        cout << "\n***" << player1.Name() << ", round" << round << endl;
        lastRoll1 = 
    }
}